# Dijkstra-Shortest-Path-Algorithm
Implemented Dijkstra’s Shortest Path Algorithm using an adjacency matrix to process weighted graph connections. Designed distance, visited, and path tracking mechanisms to identify the shortest route from a source node. Developed path reconstruction logic to display the final route between graph nodes using formatted output.
